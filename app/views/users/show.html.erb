<%= @user.nickname %>さんのユーザーページです。
  <div class="image" style="background-image: url(<%= @user.avatar.url(:medium) %>);"></div>
<% if current_user.id == @user.id %>
  <%= link_to 'プロフィール編集', edit_user_path(current_user.id) %>
<p>cssファイルを実装してないのでまだ画像はでません！すんません！
<% end %>